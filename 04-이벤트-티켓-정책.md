# 04. 이벤트/티켓 정책

## 이벤트 상태 머신

5개 상태로 구성됩니다.

```
PREPARING ──(오픈)──► OPEN ──┬──(정산)──► CALCULATING ──(종료)──► CLOSED
                             │
                             └──(삭제)──► DELETED
```

### 상태 목록

| 상태 | 코드 | 설명 |
|------|------|------|
| 준비중 | `PREPARING` | 이벤트 생성 직후 |
| 진행중 | `OPEN` | 티켓 판매 가능 |
| 정산중 | `CALCULATING` | 이벤트 종료 후 정산 처리 |
| 지난 공연 | `CLOSED` | 정산 완료 |
| 삭제됨 | `DELETED` | 소프트 삭제 |

### 오픈 조건 (PREPARING → OPEN)
1. EventBasic 정보 존재 (이름, 시간, 러닝타임)
2. EventPlace 정보 존재 (장소)
3. EventDetail 정보 존재 (상세 설명/이미지)
4. TicketItem 최소 1개 존재
5. 동일 상태 재전이 차단

### 수정 규칙
- **PREPARING**: 모든 정보 수정 가능
- **OPEN**: 기본 정보(이름/시간/장소) 수정 **불가**, 상세 정보(설명/이미지) 수정 **가능**

### 삭제 규칙
- 소프트 삭제 (status → `DELETED`)
- **OPEN 상태에서 삭제 불가**
- 발급 티켓이 존재하면 삭제 불가

---

## 티켓 유형 매트릭스

3가지 지불 유형 x 2가지 승인 유형의 조합에 엄격한 제약이 있습니다.

### PayType (지불 유형)

| PayType | 코드 | 가격 | 제약 |
|---------|------|------|------|
| 무료 티켓 | `FREE_TICKET` | 0원 강제 | - |
| 유료 티켓 | `PRICE_TICKET` | 최소 1,000원 | 선착순 필수, 제휴(partner) 호스트 필수 |
| 두둥 티켓 | `DUDOONG_TICKET` | 자유 | 승인형 필수, 계좌 정보 필수 |

### ApproveType (승인 유형)

| ApproveType | 코드 | 설명 |
|-------------|------|------|
| 선착순 | `FIRST_COME_FIRST_SERVED` | 즉시 확정 (결제 완료 시) |
| 승인 | `APPROVAL` | 관리자 수동 승인 필요 |

### 유효한 조합

| PayType | ApproveType | 가능 여부 |
|---------|-------------|----------|
| FREE_TICKET | FIRST_COME | O |
| FREE_TICKET | APPROVAL | O |
| PRICE_TICKET | FIRST_COME | O (partner 필수) |
| PRICE_TICKET | APPROVAL | X |
| DUDOONG_TICKET | APPROVAL | O (계좌 필수) |
| DUDOONG_TICKET | FIRST_COME | X |

---

## 재고 관리 정책

### 이원 수량 관리
- `quantity`: 총 발행 수량
- `supplyCount`: 남은 수량 (차감/복구 추적)

### 동시성 보호
- **Redisson 분산락**으로 재고 차감/복구 보호
- 티켓 아이템 ID 기반 락

### 구매 제한
- `purchaseLimit`: 1인당 최대 구매 수량
- 주문 생성 시 기존 주문 수량 + 신규 수량 합산 검증
- 승인형 주문은 대기 중 주문까지 포함하여 보수적 검사

### 티켓 수정/삭제 제약
- 판매 이력이 있는 티켓은 옵션 변경/삭제 **불가**

---

## 발급 티켓 (IssuedTicket) 라이프사이클

### 상태

```
ENTRANCE_INCOMPLETE ──(QR 스캔)──► ENTRANCE_COMPLETED
        │
        └──(취소/환불)──► CANCELED
```

| 상태 | 코드 | 설명 |
|------|------|------|
| 입장 전 | `ENTRANCE_INCOMPLETE` | 발급 직후 |
| 입장 완료 | `ENTRANCE_COMPLETED` | QR 스캔으로 입장 처리 |
| 취소 | `CANCELED` | 주문 취소/환불 시 연동 |

### 발급 규칙
- 주문 확정(DoneOrderEvent) 시 **자동 발급**
- 주문 철회 시 자동 취소 + 재고 복구
- **입장 완료 티켓은 취소 불가**

---

## QR 입장 처리

### 처리 플로우
```
카메라 QR 스캔 → UUID 추출 → PATCH /events/{eventId}/issuedTickets/{uuid}
```

### 검증 규칙
1. UUID로 발급 티켓 조회
2. **이벤트 ID 교차 검증** (다른 공연 QR 차단)
3. 이미 입장 완료된 티켓 → 차단
4. 취소된 티켓 → 차단
5. **매니저 이상 권한** 필요

---

## 옵션 시스템

### 3-레이어 구조

```
OptionGroup (옵션 그룹)
    └── Option (선택지)

ItemOptionGroup (티켓 ↔ 옵션그룹 연결)

IssuedTicketOptionAnswer (주문 시점 스냅샷)
```

### 옵션 유형

| 유형 | 코드 | 설명 |
|------|------|------|
| 객관식 | `SUBJECTIVE` | 미리 정의된 선택지 중 선택 |
| Y/N | `TRUE_FALSE` | 예/아니오 |
| 주관식 | `OBJECTIVE` | 자유 텍스트 입력 |

### 옵션 규칙
- 필수 옵션(`isEssential`) 미답변 시 주문 불가
- **무료 티켓에 유료 옵션(amount > 0) 적용 불가**
- 주문 시점에 답변이 스냅샷으로 보존 (이후 옵션 변경에 영향 없음)

---

## 도메인 이벤트

| 이벤트 | 발행 시점 | 처리 |
|--------|----------|------|
| `EventCreationEvent` | 이벤트 생성 시 | Slack 알림 |
| `EventStatusChangeEvent` | 상태 변경 시 | Slack 알림 |
| `EventContentChangeEvent` | 내용 수정 시 | Slack 알림 |
| `EventDeletionEvent` | 삭제 시 | Slack 알림 |
| `EntranceIssuedTicketEvent` | 입장 처리 시 | Slack 알림 |
