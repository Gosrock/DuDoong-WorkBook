# 05. 호스트/쿠폰/정산 정책

## 호스트 권한 체계

### 역할 (HostRole)

| 역할 | 코드 | 권한 범위 |
|------|------|----------|
| 방장 | `MASTER` | 모든 권한 + 역할 변경 + 멤버 추방 |
| 매니저 | `MANAGER` | 이벤트 관리, 주문 승인/취소, 입장 처리, 초대 |
| 게스트 | `GUEST` | 조회만 가능 |

### 핵심 규칙
- 방장(MASTER) 역할은 생성 후 **변경 불가**
- `@HostRolesAllowed` AOP로 선언적 권한 검사
- `partner` 플래그: 제휴 호스트 전용 기능 (`@HostPartnerAllowed`)
  - 유료 티켓 생성은 **partner 호스트만** 가능

### 멤버 관리 정책

| 동작 | 조건 |
|------|------|
| 초대 | MANAGER 이상, Redisson 분산락, 중복 가입 검증 |
| 초대 승락 | 2단계: 초대 → 본인 승락 (active 활성화) |
| 역할 변경 | MASTER만, MASTER 역할은 변경 불가 |
| 멤버 제거 | 활성 멤버 직접 제거 불가 → **비활성화 후 제거** (2단계) |

### 호스트 생성 정보
- 필수: 이름(15자 이내), 소개
- 선택: 연락처 이메일, 전화번호, 프로필 이미지
- 생성자가 자동으로 MASTER 역할

---

## 쿠폰 정책

> 현재 프로덕션에서 쿠폰 캠페인 0건 (기능 구현 완료, 미사용)

### 할인 유형

| 유형 | 코드 | 제약 |
|------|------|------|
| 정액 할인 | `AMOUNT` | 할인액 고정 |
| 정률 할인 | `PERCENTAGE` | 최대 100% |

### 적용 대상

| 대상 | 코드 | 설명 |
|------|------|------|
| 전체 | `ALL` | 모든 이벤트 (기본값) |
| 특정 | `SUB` | 특정 이벤트만 |

### 발급 규칙
- **3중 제약**: 발급 기간(start_at ~ end_at) + 재고(remainingAmount) + 유효 기간(validTerm, 일 단위)
- **Redisson 분산락** + 1인 1쿠폰 중복 발급 방지
- 쿠폰 코드: 시스템 전체 유일성 보장
- **최소 주문 금액**: 기본값 10,000원, 할인액이 초과하면 예외
- 쿠폰 생성: **SUPER_ADMIN 전용** API

### 쿠폰 사용/복구
- 주문 생성 시 쿠폰 적용 (usage_status → true)
- 주문 취소/환불 시 자동 복구 (usage_status → false)

---

## 정산 정책

### 2계층 구조

```
EventSettlement (이벤트 단위 정산)
    └── TransactionSettlement (거래 단위 정산)
            └── FeeDetail (수수료 상세)
```

### 이벤트 정산 상태

```
READY ──► CALCULATED ──► ADMIN_CONFIRM ──► CLIENT_CONFIRM
```

| 상태 | 설명 |
|------|------|
| `READY` | 정산 대기 |
| `CALCULATED` | 정산 금액 계산 완료 |
| `ADMIN_CONFIRM` | 관리자 확인 완료 |
| `CLIENT_CONFIRM` | 호스트 확인 완료 (최종) |

### 정산 금액 계산

```
총 매출 = 두둥티켓(APPROVED 주문) + 카드결제(CONFIRM 주문)

수수료 = PG 수수료 (토스페이먼츠) + VAT (PG 수수료 × 10%)

두둥 중개 수수료 = 현재 0원 (정책 미확정)

정산 금액 = 총 매출 - 수수료 - 두둥 수수료
```

### 정산 부속 자료
- 주문 목록 엑셀을 S3에 업로드
- `event_order_excel_key`로 정산 레코드에 연결

---

## 댓글 정책

| 규칙 | 내용 |
|------|------|
| 작성 자격 | 인증된 유저만 |
| 귀속 단위 | 이벤트별 |
| 내용 제한 | 최대 **200자** |
| 닉네임 제한 | 최대 **15자** |
| 삭제 방식 | 소프트 삭제 (`ACTIVE` → `INACTIVE`) |
| 삭제 검증 | 이벤트 소속 여부 재검증, 이중 삭제 방지 |
| 특수 기능 | 랜덤 댓글 조회 (`RetrieveRandomCommentUseCase`) |

---

## Slack 알림 정책

### 설정
- 호스트별 Slack 웹훅 URL **1개**
- 중복 URL 등록 차단

### 알림 종류 (총 10종)

**호스트 관련 (4종)**
- 멤버 초대/가입/추방/역할변경

**주문 관련 (6종)**
- 주문 생성
- 주문 승인/거절
- 주문 취소/환불
- 입장 처리

### 구현 방식
- 도메인 이벤트(`Events.raise()`) → 인프라 핸들러 → Slack API
- 비즈니스 로직과 알림 로직 완전 분리
