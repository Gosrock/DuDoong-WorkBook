# 03. 주문/결제 정책

## 주문 상태 머신

9개 상태, 11개 전이 규칙으로 구성됩니다.

### 상태 목록

| 상태 | 코드 | 설명 |
|------|------|------|
| 주문 생성 | `READY` | 초기 상태 |
| 결제 대기 | `PENDING_PAYMENT` | 유료/무료 선착순 주문 |
| 승인 대기 | `PENDING_APPROVE` | 두둥티켓/승인형 주문 |
| 결제 완료 | `CONFIRM` | PG 결제 확인 완료 |
| 승인 완료 | `APPROVED` | 관리자 승인 or 무료 확정 |
| 환불 완료 | `REFUND` | 사용자 환불 |
| 취소됨 | `CANCELED` | 관리자 취소 or 거절 |
| 결제 실패 | `FAILED` | 서버 오류 |
| 만료됨 | `OUTDATED` | 결제 시간 초과 (배치 처리) |

### 상태 전이 다이어그램

```
                    ┌──────────────────────────────────────┐
                    │                                      │
              유료/무료선착순                          두둥/승인형
                    │                                      │
                    ▼                                      ▼
READY ──────► PENDING_PAYMENT                    PENDING_APPROVE
                    │                                      │
        ┌───────────┼────────────┐              ┌──────────┼──────────┐
        │           │            │              │                     │
  Toss확인     0원 무료확정    서버오류      관리자 승인          관리자 거절
        │           │            │              │                     │
        ▼           ▼            ▼              ▼                     ▼
     CONFIRM    APPROVED      FAILED        APPROVED             CANCELED
        │           │
        └─────┬─────┘
              │
     ┌────────┼────────┐
     │                  │
  사용자 환불      관리자 취소
     │                  │
     ▼                  ▼
   REFUND           CANCELED
```

---

## 결제 방식 라우팅

티켓의 `PayType` + `ApproveType` 조합에 따라 자동 결정됩니다.

| PayType | ApproveType | OrderMethod | 처리 방식 |
|---------|-------------|-------------|----------|
| `PRICE_TICKET` (유료) | `FIRST_COME` (선착순) | `PAYMENT` | 토스페이먼츠 PG 결제 |
| `FREE_TICKET` (무료) | `FIRST_COME` (선착순) | `PAYMENT` | `/free` 엔드포인트 즉시 확정 |
| `FREE_TICKET` (무료) | `APPROVAL` (승인) | `APPROVAL` | 관리자 승인 대기 |
| `DUDOONG_TICKET` (두둥) | `APPROVAL` (승인) | `APPROVAL` | 무통장입금 + 관리자 승인 |

---

## 취소/환불 정책

### 3-way 분리

| 구분 | 주체 | 대상 상태 | 결과 상태 |
|------|------|----------|----------|
| **Refund** (환불) | 사용자 | CONFIRM, APPROVED | REFUND |
| **Cancel** (취소) | 관리자 | CONFIRM, APPROVED | CANCELED |
| **Refuse** (거절) | 관리자 | PENDING_APPROVE | CANCELED |

### 환불 가능 조건
- **이벤트 시작 전**에만 환불/취소 가능
- 유료 주문의 PG 환불은 `WithDrawOrderEvent` → `WithDrawOrderHandler` 비동기 처리
- 환불 시 쿠폰 복구, 발급 티켓 취소, 재고 복구 자동 수행

### 보상 트랜잭션
결제 확인 실패 시 `ConfirmOrderFailHandler`가 `AFTER_ROLLBACK` 단계에서:
1. 쿠폰 사용 취소
2. 발급 티켓 철회
3. PG 환불 요청

---

## 장바구니 정책

| 규칙 | 내용 |
|------|------|
| 유저당 장바구니 | **1개** (새로 담으면 기존 장바구니 덮어쓰기) |
| 담을 수 있는 티켓 | **단일 티켓 종류**만 (다른 종류 불가) |
| 장바구니 자동 삭제 | 주문 완료 시 자동 삭제 |

### 장바구니 생성 시 검증 (7가지)
1. 이벤트가 OPEN 상태인지
2. 티켓 판매 시간 내인지
3. 재고가 충분한지
4. 1인당 구매 제한 이내인지
5. 옵션 답변이 필수 옵션을 모두 포함하는지
6. 옵션 답변이 유효한 옵션 그룹에 속하는지
7. 티켓이 판매 가능(sellable) 상태인지

---

## 주문 생성 시 동시성 제어

- **Redisson 분산락**: `userId` 기반으로 동일 사용자의 동시 주문 차단
- **승인형 주문**: 대기 중인 주문까지 포함한 보수적 재고/구매제한 검사
- **결제 확인 시**: 토스페이먼츠에서 받은 금액과 주문 금액 일치 검증
- **탈퇴 사용자**: 승인 대기 중 탈퇴 시 승인 거부

---

## 핵심 상수

| 상수 | 값 | 설명 |
|------|---|------|
| 주문번호 형식 | `"R" + (1,000,000 + orderId)` | 예: R1000001 |
| 최소 결제금액 | 1,000원 | 유료 티켓 최소 가격 |

---

## 도메인 이벤트

| 이벤트 | 발행 시점 | 처리 |
|--------|----------|------|
| `CreateOrderEvent` | 주문 영속화 시 | Slack 알림 |
| `DoneOrderEvent` | 주문 완료 전이 시 | 발급 티켓 생성 |
| `WithDrawOrderEvent` | 주문 철회 시 | PG 환불 + 재고 복구 |
