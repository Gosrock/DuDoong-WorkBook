# 배포 구조

> 마지막 AWS 실사: 2026-03-17
> 리전: ap-northeast-2 (서울)
> ⚠️ 이 문서에 IP, 엔드포인트, SG ID 등 민감 정보를 포함하지 말 것 (퍼블릭 리포)

---

## 전체 아키텍처

```
                         ┌─────────────────┐
                         │   Route 53       │
                         │   dudoong.com    │
                         └────────┬────────┘
                                  │
                    ┌─────────────▼─────────────┐
                    │   ALB (Dudoong-LoadBalancer)│
                    │   HTTPS:443 (ACM 인증서)     │
                    │   HTTP:80 → 301 → HTTPS     │
                    └─────────────┬─────────────┘
                                  │
              ┌───────────────────┼───────────────────┐
              │ host-header 기반 라우팅                   │
              │                                        │
    ┌─────────▼──────────┐              ┌──────────────▼──────────┐
    │ dudoong.com         │              │ staging.dudoong.com     │
    │ → TG: Production    │              │ → TG: Staging           │
    │ → EC2: Production   │              │ → (서버 내려감)           │
    └─────────┬──────────┘              └─────────────────────────┘
              │
    ┌─────────▼──────────────────────────────────────────┐
    │  EC2: Dudoong-Production (t2.small)                 │
    │                                                     │
    │  Docker Compose (host networking)                   │
    │  ┌────────────────────────────────────────────┐    │
    │  │  Nginx:80 (리버스 프록시)                     │    │
    │  │  ├── /api/*    → Backend:8080              │    │
    │  │  ├── /         → Ticket:3000               │    │
    │  │  └── /admin/*  → Admin:3100                │    │
    │  ├────────────────────────────────────────────┤    │
    │  │  Backend (Spring Boot, Java 17)   :8080    │    │
    │  │  Ticket  (Next.js SSR)            :3000    │    │
    │  │  Admin   (React Vite → Nginx)     :3100    │    │
    │  │  Redis   (Alpine)                 :6379    │    │
    │  └────────────────────────────────────────────┘    │
    │          │                                          │
    └──────────┼──────────────────────────────────────────┘
               │
    ┌──────────▼──────────────────┐     ┌────────────────────────┐
    │  RDS: dudoong               │     │  CloudFront             │
    │  MySQL 8.0.42               │     │  → asset.dudoong.com    │
    │  db.t3.micro, 20GB gp3     │     │  → S3 (이미지 에셋)      │
    │  Single-AZ (2c)             │     └────────────────────────┘
    │  백업: 7일 보관              │
    └─────────────────────────────┘
```

---

## AWS 리소스 목록

### EC2 인스턴스

| 이름 | 타입 | 상태 | 용도 |
|------|------|------|------|
| Dudoong-Production | t2.small | running | 프로덕션 서버 (Docker Compose) |
| DuDoong-center | t2.micro | running | Jenkins CI + 기타 |

- 둘 다 default VPC, ap-northeast-2c 서브넷
- OS: Linux/UNIX, x86_64

### RDS

| 항목 | 값 |
|------|---|
| Identifier | `dudoong` |
| Engine | MySQL 8.0.42 |
| Class | db.t3.micro |
| Storage | 20 GB (gp3) |
| AZ | ap-northeast-2c (Single-AZ) |
| Multi-AZ | No |
| Public | Yes |
| Backup | 7일 보관 |
| 유지보수 | 일요일 18:06~18:36 UTC |

**RDS 접근 허용**:
- Production EC2 SG
- Center EC2 SG
- 개발자 로컬 IP (개별 허용)

### ALB (Application Load Balancer)

| 항목 | 값 |
|------|---|
| 이름 | Dudoong-LoadBalancer |
| Scheme | internet-facing |
| AZs | 2a, 2b, 2c, 2d (4개 서브넷) |
| SSL | `*.dudoong.com` 와일드카드 (ACM, ~2026-11-02 만료) |

**리스너 규칙 (HTTPS:443)**:

| 우선순위 | 조건 (host-header) | 대상 타겟 그룹 |
|---------|-------------------|--------------|
| 1 | `pwa.dudoong.com` | PWA → Production EC2 |
| 2 | `jenkins.dudoong.com` | Dudoong-jenkins → Center EC2:8080 |
| 3 | `staging.dudoong.com` | Dudoong-Staging → (서버 내려감) |
| 4 | `dudoong.com` | Dudoong-Production → Production EC2 |
| default | 나머지 | Dudoong-Staging |

HTTP:80 → 301 리다이렉트 → HTTPS:443

**타겟 그룹**:

| 이름 | 포트 | Health Check | 용도 |
|------|------|-------------|------|
| Dudoong-Production | 80 | `/api/v1/examples/health` (300s) | 프로덕션 |
| Dudoong-Staging | 80 | `/api/v1/examples/health` (30s) | 스테이징 (내려감) |
| Dudoong-jenkins | 8080 | `/` (30s) | Jenkins CI |
| PWA | 80 | `/` (30s) | PWA 앱 |

### 이미지 레지스트리

> 현재 DuDoong 이미지는 **Docker Hub** 사용 (ECR 아님)

### SES (이메일)

- `dudoong.com` DKIM 설정 완료
- 이메일 수신 설정 있음 (AWS SES inbound)

---

## 도메인 구조 (Route 53)

### dudoong.com

| 레코드 | 타입 | 대상 | 용도 |
|--------|------|------|------|
| `dudoong.com` | A (Alias) | ALB | 관객 앱 (ticket) |
| `staging.dudoong.com` | A (Alias) | ALB | 스테이징 (내려감) |
| `pwa.dudoong.com` | A (Alias) | ALB | PWA 앱 |
| `jenkins.dudoong.com` | A (Alias) | ALB | Jenkins CI |
| `asset.dudoong.com` | A (Alias) | CloudFront | 이미지 에셋 CDN |

### gosrock.band

> 이전 서비스(Gosrock). SSL 인증서 만료됨 (2023-08). 사실상 미사용.

---

## Security Groups (역할 기준)

| 역할 | 인바운드 포트 | 사용처 |
|------|-------------|--------|
| Production EC2 | 22, 80, 443 | 프로덕션 서버 |
| Center EC2 | 22, 80, 443, 8080, 8000 | Jenkins/기타 서버 |
| RDS | 3306 (Production SG + Center SG + 개발자 IP) | MySQL |

---

## 네트워크 구조

```
Default VPC (172.31.0.0/16)
├── ap-northeast-2a
├── ap-northeast-2b
├── ap-northeast-2c ← EC2, RDS 위치
└── ap-northeast-2d
```

- Default VPC 사용 (커스텀 VPC 없음)
- 모든 서브넷 퍼블릭
- EC2, RDS 모두 ap-northeast-2c

---

## 서비스 포트 배치

| 서비스 | 포트 | 이미지 | 비고 |
|--------|:----:|--------|------|
| Nginx (리버스 프록시) | 80 | `water0641/dudoong-nginx:1.0.5` | 엔트리 포인트 |
| API Backend | 8080 | `water0641/dudoong-api:1.0.7-2` | Spring Boot (Java 17) |
| Ticket App | 3000 | `david0218/dudoong-ticket:1.3.1` | Next.js SSR |
| Admin Panel | 3100 | `david0218/dudoong-admin:1.1.2` | React (Vite → Nginx) |
| Socket Server | 8090 | Spring WebSocket | STOMP 실시간 |
| Redis | 6379 | `redis:alpine` | 캐시/세션/분산락 |

> 모든 서비스 `network_mode: "host"` 사용 (Docker bridge 아님)

---

## Nginx 라우팅 (default.conf)

```nginx
server {
    listen 80;
    server_name dudoong.com staging.dudoong.com;

    location /api    → localhost:8080  (+ WebSocket upgrade 지원)
    location /       → localhost:3000  (Ticket 프론트)
    location /admin/ → localhost:3100  (Admin 프론트)
}
```

- SSL 터미네이션은 ALB에서 처리 (Nginx는 HTTP:80만)

---

## 환경별 비교

| 항목 | Production | Staging (내려감) |
|------|-----------|----------------|
| 도메인 | dudoong.com | staging.dudoong.com |
| EC2 | Dudoong-Production (t2.small) | (별도 인스턴스 없음) |
| ALB TG | Dudoong-Production | Dudoong-Staging (타겟 없음) |
| API 이미지 | `dudoong-api:1.0.7-2` | `dudoong-api:1.0.6-3` |
| FE 이미지 | `dudoong-ticket:1.3.1` | `dudoong-ticket:dev` |
| Admin 이미지 | `dudoong-admin:1.1.2` | `dudoong-admin:dev` |
| 로깅 | CloudWatch (`dudoong-prod`) | 없음 |
| Health Check | 300s 간격 | 30s 간격 |
| 프로필 | `SPRING_PROFILES_ACTIVE=prod` | `staging` |

---

## CI/CD 파이프라인

> **CI/CD는 DuDoong-Deploy 리포를 통해 운영 중**
> GitHub: https://github.com/Gosrock/DuDoong-Deploy

### 이미지 빌드 (GitHub Actions)

**태그 컨벤션**:
| 서비스 | 태그 형식 | 예시 |
|--------|----------|------|
| Backend API | `Api-v*.*.*` | `Api-v1.0.7` |
| Backend Batch | `Batch-v*.*.*` | `Batch-v1.0.1` |
| Backend Socket | `Socket-v*.*.*` | `Socket-v1.0.1` |
| Frontend Ticket | `Ticket-v*.*.*` | `Ticket-v1.3.1` |
| Frontend Admin | `Admin-v*.*.*` | `Admin-v1.1.2` |

**백엔드 빌드**:
```
태그 push → GitHub Actions → Java 17 setup → Redis docker up (테스트) →
./gradlew :Module:build → Docker Hub push
```

**프론트엔드 빌드**:
```
태그 push → GitHub Actions → .env 생성 (Secrets) →
Docker Buildx (linux/amd64) → Docker Hub push
```

### 배포 (DuDoong-Deploy → EC2)

```
DuDoong-Deploy main push → deploy.yml →
.env 생성 → SSH 접속 → docker-install.sh →
rsync 파일 동기화 → docker-compose pull → up --build -d → prune
```

- 이미지 레지스트리: **Docker Hub** (ECR 아님)
- 시크릿 관리: GitHub Secrets → 배포 시 `.env` 파일 생성
- 배포 설정: `docker-compose.prod.yml`, `docker-compose.staging.yml`, `nginx/`
- **스테이징**: main push 시 자동 배포
- **프로덕션**: 수동 트리거 (`deploy-live.yml`)

---

## 배포 환경 재구성 시 체크리스트

스테이징 서버를 다시 올리거나, 새 환경을 구성할 때:

### 1. EC2 인스턴스
- [ ] 인스턴스 생성 (t2.small 이상 권장, ap-northeast-2c)
- [ ] SG: 22, 80, 443 인바운드 허용
- [ ] SSH 키 페어 설정
- [ ] Docker + Docker Compose 설치 (`docker-install.sh`)

### 2. ALB 타겟 그룹
- [ ] 기존 TG `Dudoong-Staging`에 새 인스턴스 등록
- [ ] 또는 새 TG 생성 후 ALB 리스너 규칙 업데이트
- [ ] Health Check: `/api/v1/examples/health`

### 3. RDS 접근
- [ ] 새 EC2의 SG를 RDS SG 인바운드에 추가
- [ ] 또는 기존 Production SG 재사용

### 4. 환경변수
- [ ] GitHub Secrets에서 `.env` 파일 생성 (또는 수동 복사)
- [ ] `SPRING_PROFILES_ACTIVE=staging`
- [ ] DB, Redis, OAuth, PG, AWS, Slack, NCP 키 설정

### 5. Docker Compose
- [ ] `docker-compose.staging.yml` 이미지 태그 업데이트
- [ ] `docker-compose pull && docker-compose up -d`

### 6. DNS (이미 설정됨)
- [x] `staging.dudoong.com` → ALB (Route 53 A 레코드 존재)

### 7. SSL (이미 설정됨)
- [x] `*.dudoong.com` 와일드카드 인증서 (ACM, 2026-11-02 만료)

---

## 환경 변수 구조

### 공통
- `TZ=Asia/Seoul`
- `SPRING_PROFILES_ACTIVE` (dev/staging/prod)

### 백엔드 주요 환경변수

| 카테고리 | 변수명 |
|----------|--------|
| JWT | `JWT_SECRET_KEY`, `JWT_ACCESS_EXP`, `JWT_REFRESH_EXP` |
| OAuth | `KAKAO_CLIENT`, `KAKAO_SECRET`, `KAKAO_REDIRECT`, `KAKAO_APP_ID`, `KAKAO_ADMIN_KEY` |
| PG | `TOSS_PAYMENTS_KEY`, `TOSS_MID` |
| AWS | `AWS_ACCESS_KEY`, `AWS_SECRET_KEY`, `AWS_S3_BUCKET`, `AWS_S3_BASE_URL` |
| Redis | `REDIS_HOST`, `REDIS_PORT`, `REDIS_PASSWORD` |
| Slack | `SLACK_WEBHOOK_TOKEN`, `SLACK_WEBHOOK_ID`, `SLACK_SERVICE_CHANNEL_ID` |
| NCP | `NCP_SERVICE_ID`, `NCP_ACCESS_KEY`, `NCP_SECRET_KEY`, `NCP_PLUS_FRIEND_ID` |
| 보안 | `ACL_WHITELIST`, `SWAGGER_USER`, `SWAGGER_PASSWORD` |

### 시크릿 관리
- GitHub Secrets → 배포 시 `.env` 파일 생성
- 코드/이미지에 시크릿 포함하지 않음

---

## 모니터링

| 환경 | 도구 | 설정 |
|------|------|------|
| 프로덕션 | AWS CloudWatch Logs | `dudoong-prod` / `backend` |
| 스테이징 | 없음 | 미구성 |

> Health check, Prometheus/Grafana, 분산 트레이싱 미구성

---

## Dockerfile 구조

### Backend (Java 17)
```dockerfile
FROM openjdk:17-ea-slim
COPY build/libs/*.jar app.jar
ENTRYPOINT ["java", "-Dspring.profiles.active=${SPRING_PROFILES_ACTIVE}", "-jar", "app.jar"]
```

### Frontend Ticket (Next.js)
```dockerfile
FROM node:18-alpine AS builder → build → FROM node:18-alpine (SSR)
```

### Frontend Admin (Vite → Nginx)
```dockerfile
FROM node:18-alpine AS builder → build → FROM nginx:1.19.0 (정적 서빙)
```

---

_이 문서는 AWS API 실사 데이터 + DuDoong-Deploy 리포지토리 기반으로 작성되었습니다._
